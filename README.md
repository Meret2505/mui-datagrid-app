# 📚 Книжный каталог — «Читальный зал»

React-приложение, демонстрирующее работу с библиотекой компонентов
**Material UI** и таблицей **DataGrid** (`@mui/x-data-grid`). Данные о книгах
подгружаются из открытого **Open Library API**.

Интерфейс оформлен в едином **редакционном стиле «читальный зал»**: тёплая
бумага, цвет «бычья кровь» и латунные акценты, заголовочный шрифт высокого
контраста и полностью **русскоязычный интерфейс** (включая тулбар, пагинацию и
фильтры самой DataGrid через локали `ruRU`).

## 🔗 Демо

**[▶ Открыть приложение](https://shiny-unicorn-2dcf40.netlify.app)** — развёрнуто на Netlify.

## ✨ Возможности

### Основное

- ⚛️ **React 18** + **Material UI 6** + **Vite**.
- 📊 Таблица на компоненте **`DataGrid`** из MUI.
- 🧱 **6 колонок**: обложка (картинка), название и автор (текст), дата издания
  (дата), рейтинг и количество страниц (числа).
- 🔍 Все колонки **сортируются и фильтруются** (плюс быстрый поиск в тулбаре).
- 🎨 У текстовых колонок **разные стили**: название — контрастный serif
  (Playfair Display), автор — курсив в латунном цвете secondary, дата —
  моноширинный шрифт (JetBrains Mono).
- 📐 **Автоматическая высота строк** (`getRowHeight: 'auto'`) с ограничением
  `min 100px / max 300px` через CSS.
- 🖱️ **Клик по строке** → модальное окно с подробной информацией о книге.
- 🖼️ **Клик по обложке** → модальное окно с **увеличенным изображением**.
- 🧩 Использованы и **функциональные** компоненты (`RowDetailModal`,
  `BooksTable`, `App`), и **классовый** компонент (`ImageModal`).

### Бонусы ✅

- 💾 **Сохранение состояния таблицы** (сортировка, фильтры, пагинация) в
  `localStorage` — переживает перезагрузку страницы.
- 🌗 **Переключение тёмной / светлой темы** (тоже сохраняется).
- 🌐 Реальные данные из **открытого API** (Open Library).

### Дизайн 🎨

- 🏛️ **Редакционная эстетика «читальный зал»**: пергаментный фон с зерном,
  цвет «бычья кровь» (oxblood) и латунь вместо стандартного синего MUI.
- 🔤 **Авторская типографика**: Playfair Display (заголовки) · Golos Text
  (интерфейс, кириллица) · JetBrains Mono (даты и числа).
- 🇷🇺 **Полностью русский интерфейс** — тулбар, пагинация и операторы фильтров
  DataGrid локализованы через `ruRU` (`@mui/x-data-grid/locales` +
  `@mui/material/locale`).
- ✨ Анимация появления шапки, живые счётчики (число изданий и средний рейтинг),
  адаптивная вёрстка.

## 🛠️ Стек

| Технология          | Назначение                       |
| ------------------- | -------------------------------- |
| React 18            | UI-библиотека                    |
| Material UI 6       | Компоненты и темизация           |
| @mui/x-data-grid 7  | Таблица данных                   |
| Vite 5              | Сборка и dev-сервер              |
| Open Library API    | Источник данных о книгах         |

## 🚀 Запуск локально

```bash
npm install
npm run dev      # http://localhost:5173
```

Сборка production-версии:

```bash
npm run build
npm run preview
```

## 📁 Структура проекта

```
src/
├── App.jsx                     # корневой компонент, тема, шапка
├── main.jsx                    # точка входа
├── theme/
│   └── theme.js                # фабрика light/dark темы MUI
├── hooks/
│   ├── usePersistentState.js   # useState + localStorage
│   └── useBooks.js             # загрузка данных из Open Library API
└── components/
    ├── BooksTable.jsx          # DataGrid с колонками и логикой
    ├── RowDetailModal.jsx      # модалка строки (функц. компонент)
    └── ImageModal.jsx          # модалка картинки (классовый компонент)
```

## 🌍 Деплой

### Netlify

1. Подключите репозиторий на [netlify.com](https://app.netlify.com).
2. Build command: `npm run build`, publish directory: `dist`
   (уже задано в `netlify.toml`).

### GitHub Pages

В репозитории есть workflow `.github/workflows/deploy.yml`. Включите Pages
(Settings → Pages → Source: GitHub Actions), и при пуше в `main` приложение
задеплоится автоматически.

## 📝 Лицензия

MIT
